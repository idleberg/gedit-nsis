name: CI

on:
  push:
    paths:
      - "*.lang"
  pull_request:
    paths:
      - "*.lang"
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate XML
        run: xmllint --noout *.lang
